# OTP Verification System

This project will eventually provide a beginner-friendly system for sending and verifying one-time passwords (OTPs).

For now, it contains only a basic Node.js and Express server. OTP verification and the frontend will be added in later steps.

## Start the application

```bash
npm start
```

Then open <http://localhost:3000> in a browser.